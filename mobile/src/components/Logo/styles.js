import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    box: {
        margin: 20,
        borderRadius: 20,
        height:100,
        // width:150,
        padding:20,
        // backgroundColor: '#fff',
        borderColor: '#4CBC4C',
        borderWidth: 1,
        alignItems: 'center',
        justifyContent: 'center',
    },
    title: {
        color: '#4CBC4C',
        fontWeight: 'bold',
        fontSize: 24,
    }
});
